[pytest]
markers =
    smoke: Quick sanity checks — run before every deployment
    regression: Full regression suite — run before every release
    bug: Tests that prove bugs exist in the simulator
    positive: Tests that verify correct behaviour
    negative: Tests that verify rejection of invalid input

addopts = -v --tb=short

testpaths = tests